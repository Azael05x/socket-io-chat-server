export const KICK_SILENT_MIN = 5;
export const KICK_SILENT_MESSAGE = (username: string) => `${username} was disconnected due to inactivity`;

export const DISCONNECTED_MESSAGE = (username: string) => `${username} left the chat, connection lost`;
